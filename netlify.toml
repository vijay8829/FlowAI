[build]
  base = "frontend"
  command = "npm install && ./node_modules/.bin/vite build"
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
